# version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - VITE_BASE_URL=${VITE_BASE_URL}
        - VITE_BASE_PATH=${VITE_BASE_PATH}
    ports:
      - "1412:1412"
    environment:
      - NODE_ENV=production
    # volumes:
    #   - .:/app
    #   - /app/node_modules
    networks:
      - linklian-net

networks:
  linklian-net:
    external: true